# History

[2026-06-18] docs: tạo shared context workspace cho Multi-Site CMS Publisher
[2026-06-18] docs: chuẩn hóa project summary, active sprint, file index, decisions, errors
[2026-06-18] docs: thêm adapter `AGENTS.md` và `CLAUDE.md` để Codex CLI và Claude CLI dùng chung context
[2026-06-18] chore: thêm `.claude/` rút gọn với hooks, commands, agents áp dụng từ Multi-AI-Workspace-Setup
[2026-06-18] feat(web): hoàn thành Phase 1 Web foundation (Agent B) - Serilog, cookie auth qua minimal-API endpoints, admin shell layout, toast service, Users CRUD (Admin). Build sạch, app khởi động, migrate/seed OK, login flow đã xác thực.
[2026-06-18] chore(scaffold): tạo solution `MultiSiteCmsPublisher.slnx` với 3 project (`Publisher.Web` Blazor Server + minimal API, `Publisher.Core`, `Publisher.Infrastructure`) + `Publisher.Tests`, target net8.0.
[2026-06-18] feat(core+infra): Phase 1 foundation (Agent A) - entities mirror `database-design.sql`, AppDbContext + Fluent config, migration InitialCreate (kèm views/SPs qua raw SQL), seed admin/prompt templates, ConnectionStringEncryptor AES-256-GCM, AuditLogService. Migration applied to LocalDB.
[2026-06-18] feat(infra): Phase 2 backend (Agent C) - SafeIdentifier whitelist, SiteDbConnector (Dapper, INFORMATION_SCHEMA, test-conn + permission probe), InsertCommandBuilder parameterized + SCOPE_IDENTITY. 39 unit tests.
[2026-06-18] feat(web): Phase 2 frontend (Agent D) - Site CRUD (encrypt conn string, test-before-save), field mapping UI (table/column dropdowns, custom fields, INSERT preview, validate test-insert-rollback).
[2026-06-18] feat(web+infra): Phase 3 (Agent E) - PostPublisher (build INSERT từ mapping, Dapper execute trong transaction, capture RemotePostId, status transitions, audit), Post list/editor (Quill, auto-slug bỏ dấu tiếng Việt), Publish Now / Schedule (UTC) / Retry.
[2026-06-18] test(e2e): Wave 4 verification - integration test publish thật vào LocalDB `TargetSiteDemo.dbo.Articles` (row inserted, RemotePostId captured, draft→published), injection bị SafeIdentifier chặn. Build sạch, 56 tests pass, app start + /login OK.
[2026-06-18] fix(web): thêm `@using Publisher.Core.Models` vào `SiteEdit.razor` (race giữa agent song song gây CS0246).
[2026-06-18] chore(config): trỏ AppDb sang SQL Server `localhost/01MultiSiteCMS` (instance mặc định, Windows auth). AppDbContextFactory đọc env `ConnectionStrings__AppDb` (mặc định DB mới). Áp migration: 7 bảng + 2 view + 2 proc; app start OK, seed (admin + prompt templates) đã ghi, /login=200.
[2026-06-18] chore(git): review (build sạch, 56/56 test pass) rồi merge `feature/phases-1-3-foundation` vào `main` (--no-ff, commit fcb2b17). Chưa push origin.
[2026-06-19] docs: rà soát Phase 3 (đối chiếu DoD: post editor/PostPublisher/publish-schedule-retry đều có code + E2E pass) → kết luận hoàn thiện; tổng hợp scope Phase 4 (AIContentGenerator streaming, Prompt Template Manager, AI trong editor, kiểm soát token) từ deployment-plan.md. Lưu ý 2 việc treo: chưa push origin, chưa xóa nhánh feature.
[2026-06-19] chore(git): push `main` lên origin (e6829c2); xác nhận nhánh `feature/phases-1-3-foundation` đã xóa ở local+remote từ trước.
[2026-06-19] decision(D-006): Phase 4 gọi qua dịch vụ "AI Gateway" riêng wrap codex CLI (Claude/Gemini fallback), JSON v1, fallback trong gateway. Plan: memoized-mixing-kahn.md.
[2026-06-19] feat(ai-gateway): scaffold dịch vụ Node 20 + TS + Fastify (M1-M3 phía service) — config zod, Bearer auth (constant-time), CodexProvider (spawn codex exec --json --sandbox read-only, parse JSONL), Claude+Gemini provider, Orchestrator fallback + dịch song song (p-limit), POST /v1/generate + /healthz + /v1/providers, error handler. 19/19 vitest pass, typecheck sạch, tsup build OK, smoke test server thật OK. Kèm Dockerfile + systemd unit + nginx TLS conf. CMS-side (M4) chưa làm.
[2026-06-19] feat(multilang): đa ngôn ngữ cho Site + Post (D-007). P1 data model: Site.SupportedLanguagesJson, Post.Language+TranslationGroupId (index), SiteFieldMapping.LocalizedColumnsJson; migration AddMultiLanguage (applied vào 01MultiSiteCMS). P4 publish: InsertCommandBuilder.BuildLocalized (nhóm dịch → 1 INSERT cột-theo-ngôn-ngữ, SafeIdentifier) + PostPublisher gom nhóm lưu RemotePostId/status cả nhóm (site đơn ngôn ngữ giữ Build() cũ); 7 test mới. P2 site UI: SiteEdit chọn nhiều ngôn ngữ + ngôn ngữ chính; SiteMapping ma trận cột-theo-ngôn-ngữ + preview BuildLocalized. P3 PostEditor: tabs theo ngôn ngữ (1 Quill editor/ngôn ngữ), lưu nhóm Post cùng TranslationGroupId, publish/schedule theo nhóm, AI Viết sinh gốc + dịch lấp tab. Build 3 project sạch, 70/70 unit test, app boot :5099 OK. Còn P5: E2E publish lên bảng remote có cột *_vi/*_en (người dùng test trên browser).
[2026-06-19] feat(ai-gateway): chạy THẬT với codex CLI (0.141) — verify chất lượng bài. Cài @openai/codex global; auth qua CODEX_HOME riêng (C:/Users/trung/.codex-aigw) bằng `codex login --with-api-key` (env OPENAI_API_KEY đơn lẻ → 401). Fix nhiều điểm cho codex 0.141: bỏ flag `--ask-for-approval` (không tồn tại), truyền prompt qua STDIN (tránh treo chờ stdin + lỗi quote), spawn `shell:true` trên Windows để gọi codex.cmd, không ép model (-m chỉ khi CODEX_MODEL set), không clobber env key rỗng, CODEX_HOME/CODEX_ENABLED/CODEX_MODEL config, dotenv nạp .env. Gemini đổi model gemini-2.0-flash→2.5-flash (cũ 404). Claude prefill '{' ép JSON. Kết quả: codex sinh bài VI SEO chất lượng cao (~22-25s, vd 13k tokens) + dịch EN tốt; fallback codex→claude→gemini hoạt động khi codex timeout. KEYS: chuyển từ .env.example (đã lỡ commit dạng rỗng) sang .env gitignored; .env.example trả về placeholder. 19/19 gateway test pass.
[2026-06-19] test(e2e-local): dựng gateway local để verify Phase 4 không cần VPS/key. Thêm MockProvider (gateway, bật bằng MOCK_PROVIDER=true + PROVIDER_ORDER=mock) sinh bài/dịch giả lập deterministic. Fix: AIContentService bỏ qua null khi serialize (DefaultIgnoreCondition.WhenWritingNull) + gateway zod nhận null cho field optional (.nullish). Integration test AIGatewayIntegrationTests (CMS AIContentService → gateway thật, gated bằng AIGATEWAY_TEST_URL) PASS. Verify: gateway /v1/generate trả bài+2 dịch (provider=mock); app CMS boot sạch trên :5099 (DB migrate/seed OK, AI bật, /login=200, /=302); gateway :8080 healthz=200. Gateway 19/19 + CMS 63/63 unit pass. appsettings.Development.json (gitignored) trỏ AIGateway→localhost:8080.
[2026-06-19] feat(web+infra): Phase 4 M4 — tích hợp CMS với AI Gateway. Core: IAIContentService + models (AIGenerateRequest/Result/Article/Usage) + AIGatewayOptions + AIGatewayException. Infra: AIContentService (typed HttpClient, camelCase JSON, map error envelope) + PromptTemplateRenderer ({topic}/{keywords}/{length}/{tone}/{site_name}); DI: Configure<AIGatewayOptions> (env PUBLISHER_AIGATEWAY_URL/KEY ưu tiên) + AddHttpClient; thêm package Microsoft.Extensions.Http. Web: Prompt Template Manager (TemplateList/TemplateEdit + Test prompt, Admin) + nav link; nút "AI Viết" trong PostEditor (topic/keywords/template/tone/length/translateTo → đổ vào Quill, set IsAIGenerated/AIPromptUsed/AITokensUsed, tạo nháp bản dịch cho site). appsettings: section AIGateway. Build 3 project sạch; 63/63 unit test pass (+9 AI). Chưa chạy app thật (cần DB+gateway), chưa cấu hình URL/key.
